{
  "name": "travis_gh-pages",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Hello world\"",
    "dev": "parcel serve index.js --open",
    "build": "rm -rf build/* && parcel build $npm_package_main -d build --public-url /"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
